# Continuum Function Fitting

This code fits continuum functions for arbitrary types of distorted potentials.

It uses the [RADIAL](http://users.df.uba.ar/dmitnik/estructura3/programas/salvat/radial.pdf) package to generate continuum functions for arbitrary types of distorted potentials.

